# Package tests for cis.
# Note: these tests use package imports like `cis.main_detector`,
# so they should be executed from the repository root with:
#   python -m unittest discover -s cis/tests -p "test_*.py" -v
